# Images

To update the site avatar:

1. Replace `avatar.jpg` in this folder with your new photo (use the same filename).
2. From the project root run:

   ```bash
   git add images/avatar.jpg
   git commit -m "Update avatar photo"
   git push origin main
   ```

The site uses this image at [index.html](../index.html) (line 71).
